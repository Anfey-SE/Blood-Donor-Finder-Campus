\# Campus Blood Donor Finder



\## Project Description

Campus Blood Donor Finder is a web application that connects blood donors and requesters within a campus. It allows students to register as donors, submit blood requests, search for donors by blood group, and manage their donor profiles. Admins can verify profiles and ensure the integrity of the system.



\## Features Implemented in Sprint 1

\- User registration and login system

\- Donor profile creation and update

\- Blood request submission form

\- Search for donors by blood group

\- Simulated donor notifications

\- Admin verification of profiles

\- Password reset functionality

\- Mark donor availability

\- Track donation history



\## Technologies Used

\- HTML, CSS for frontend

\- PHP for backend logic

\- Text files (`donors.txt`, `requests.txt`, `history.txt`) as simple data storage

\- XAMPP for local server



\## Installation \& Running

1\. Install XAMPP on your machine.

2\. Copy the project folder to `C:/xampp/htdocs/`.

3\. Start Apache from XAMPP Control Panel.

4\. Open your browser and navigate to: `http://localhost/your\\\_project\\\_folder\\\_name/index.html`



\## Project Structure

\- `index.html` – Home page

\- `register.html` / `register.php` – Registration

\- `login.html` / `login.php` – Login

\- `profile.html` / `profile.php` – Donor profile update

\- `request.html` / `request.php` – Blood request submission

\- `search.html` / `search.php` – Donor search

\- `reset.html` / `reset.php` – Password reset

\- `availability.html` / `availability.php` – Mark donor availability

\- `admin.html` / `admin.php` – Admin profile verification

\- `history.html` / `history.php` / `viewhistory.php` – Donation history

\- `style.css` – Stylesheet

\- `donors.txt`, `requests.txt`, `history.txt` – Data files



\## Author

\- Your Name

\- Student ID (optional)

\- Contact Email (optional)

